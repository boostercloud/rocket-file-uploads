import { BoosterConfig } from '@boostercloud/framework-types'

export async function presignedPut(config: BoosterConfig, directory: string, fileName: string): Promise<string> {
  return 'Local post url'
}
